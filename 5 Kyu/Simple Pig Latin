def pig_it(text):
    
    result = []
    
    for word in text.split(" "):
        
        if word in "?!":
            result.append(word)
            
        else:
            result.append(word[1:] + word[0] + "ay")
            
    return " ".join(result)
