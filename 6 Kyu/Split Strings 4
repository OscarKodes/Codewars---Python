def solution(s):
    
    result = []
    
    for i in range(0, len(s), 2):
        
        pair = s[i] + "_" if i + 1 == len(s) else s[i:i+2]
        
        result.append(pair)
        
    return result
