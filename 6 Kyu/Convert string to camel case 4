def to_camel_case(text):
    
    onlyUnders = text.replace("-", "_")
    
    ls = [(s[0] if i == 0 else s[0].upper()) + s[1:] for i, s in enumerate(onlyUnders.split("_")) if len(s) > 0]
    
    return "".join(ls)
