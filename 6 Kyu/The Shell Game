def find_the_ball(start, swaps):
    
    ball = start
    
    for swap in swaps:
        
        if ball in swap:
            
            ls = list(swap)
            ls.remove(ball)
            ball = ls[0]
            
    return ball
