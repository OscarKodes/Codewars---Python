def divisors(integer):
    
    result = []
    
    for i in range(2, integer, 1):
        
        if integer % i == 0:
            
            result.append(i)
            
    return result if len(result) > 0 else f'{integer} is prime'
