def sum_of_n(n):
    
    result = []

    for i in range(0, n + 1 if n > 0 else n - 1, 1 if n > 0 else -1):
        
        result.append((0 if i == 0 else result[len(result) - 1]) + i)
        
    return result
