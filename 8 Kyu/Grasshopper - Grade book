def get_grade(s1, s2, s3):
    
    mean = sum([s1, s2, s3]) / 3
    grades = [60, 70, 80, 90, 100]
    
    for i, grade in enumerate(grades):
        if mean < grade:
            return "FDCBA"[i]
        
    return "A"
